#include <stdio.h>
#include <stdlib.h>
#include <sys/types.h>

int main (){
char *line = NULL;
size_t len ;
ssize_t nread;

nread = getline(&line,&len,stdin);
fwrite(line,sizeof(char),nread,stdout);
return 0;
}
