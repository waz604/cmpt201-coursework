#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main() {
  int maxw= 5;
  int maxl= 30;
  char words [maxw][maxl];
  char *string = NULL;
  size_t len = 0;
  size_t nread;

  
  while (strcmp(string,"print") != 0) {
    printf("Enter input: ");
    nread= getline(&string, &len, stdin);
    printf("%s", string);
  }

  return 0;
}
