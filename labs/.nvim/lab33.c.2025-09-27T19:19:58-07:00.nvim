#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/types.h>
int main() {
  char *line = NULL;
  size_t len;
  ssize_t nread;
  while (1) {
    nread = getline(&line, &len, stdin);
    fwrite(line, sizeof(char), nread, stdout);
    if(strcmp(line,"print")){
        break;
        }
  }
  return 0;
}
