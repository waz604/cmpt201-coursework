#include <stdio.h>
#include <string.h>
#include <sys/wait.h>
#include <unistd.h>
int main() {
  pid_t pid = fork();
  if (pid == 0) {
    char str[100];
    printf("Enter programs to run");
    fgets(str, sizeof(str), stdin);
    printf("%s", str);
  }
}
